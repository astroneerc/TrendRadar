name: Build and Push Multi-Arch Docker Images

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: wantcat/trendradar

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            network=host

      - name: Login to Docker Hub
         run: |
    echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  env:
    DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_dej00798 }}         # 从 Secrets 注入
    DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_dckr_pat_Ih4OCqPFFiR3V1tpeLmI5dPARww }}             # 从 Secrets 注入
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_dej00798 }}
          password: ${{ secrets.DOCKERHUB_dckr_pat_Ih4OCqPFFiR3V1tpeLmI5dPARww }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: dej00798/trendradar:latest
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        env:
          BUILDKIT_PROGRESS: plain
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1
